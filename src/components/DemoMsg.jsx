import React from "react";

const DemoMsg = () => {
  const demo = {
    display: " flex",
    justifyContent: "center",
    alignItem: "center",
  };
  return (
    <div style={demo}>
      <h3 style={{ padding: "155px" }}>
        it's just demo project for practice and the data i fetched for shoses
        only
      </h3>
    </div>
  );
};

export default DemoMsg;
